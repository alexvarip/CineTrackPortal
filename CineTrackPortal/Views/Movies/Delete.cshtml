@model CineTrackPortal.Models.MovieModel

@{
    ViewData["Title"] = "Delete Movie";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger shadow">
                <div class="card-header bg-danger text-white">
                    <h2 class="mb-0"><i class="bi bi-exclamation-triangle-fill"></i> Delete Movie</h2>
                </div>
                <div class="card-body">
                    <h4 class="text-danger mb-4">Are you sure you want to delete this movie?</h4>
                    @Html.ValidationSummary(true, "", new { @class = "text-danger mb-3" })
                    <dl class="row">
                        <dt class="col-sm-3">Title</dt>
                        <dd class="col-sm-9">@Model.Title</dd>
                        <dt class="col-sm-3">Date</dt>
                        <dd class="col-sm-9">@Model.Date.ToShortDateString()</dd>
                    </dl>
                    <div class="alert alert-warning mt-4" role="alert">
                        <strong>Warning:</strong> Deleting this movie will also delete all associated actors.
                    </div>
                    <form asp-action="Delete" method="post" id="deleteForm" class="mt-3">
                        <input type="hidden" asp-for="MovieId" />
                        <button type="submit" class="btn btn-danger me-2">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left"></i> Cancel
                        </a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.getElementById('deleteForm').addEventListener('submit', function (e) {
            var confirmed = confirm('Are you really really really sure you want to proceed?');
            if (!confirmed) {
                e.preventDefault();
            }
        });
    </script>
}